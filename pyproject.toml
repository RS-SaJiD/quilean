[project]
name = "autofile"
version = "1.0.0"
description = "A smart CLI tool to organize, rename and clean files"
authors = [{name = "Your Name", email = "your@email.com"}]
dependencies = [
    "click>=8.1.0",
    "rich>=13.0.0",
    "questionary>=2.0.0",
    "colorama>=0.4.6"
]
requires-python = ">=3.8"
readme = "README.md"

[project.scripts]
autofile = "autofile.cli:main"
